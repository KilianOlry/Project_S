<form method="post" action="{{ path('app_admin_offre_assurance_delete', {'id': offre_assurance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre_assurance.id) }}">
    <button class="btn">Delete</button>
</form>
