{% extends 'base.html.twig' %}

{% block title %}OffreAssurance{% endblock %}

{% block body %}
    <h1>OffreAssurance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre_assurance.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ offre_assurance.titre }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ offre_assurance.slug }}</td>
            </tr>
            <tr>
                <th>Fichier</th>
                <td>{{ offre_assurance.fichier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_offre_assurance_index') }}">back to list</a>

    <a href="{{ path('app_admin_offre_assurance_edit', {'id': offre_assurance.id}) }}">edit</a>

    {{ include('admin_offre_assurance/_delete_form.html.twig') }}
{% endblock %}
